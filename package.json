{
  "name": "ainumbers-site",
  "private": true,
  "description": "Dev/CI-only tooling deps for the AINumbers site repo. Product code (tools/guides/kernels) stays zero-dependency per CONTRACT.md; this file exists solely to pin Ajv for scripts/check-manifest-schema.mjs (SSOT-SCHEMA-1).",
  "devDependencies": {
    "ajv": "^8.17.1"
  }
}
