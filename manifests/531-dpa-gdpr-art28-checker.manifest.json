{
  "tool_id": "dpa-gdpr-art28-checker",
  "version": "1.0.0",
  "title": "DPA GDPR Art 28(3) Checker",
  "description": "Checks a pasted DPA against GDPR Art 28(3) 12-point mandatory processor clause checklist. Returns present/missing/weak per clause and overall completeness score. Policy Mandate export. Selection and analysis only, not legal advice.",
  "category": "cat-31",
  "tags": ["DPA", "GDPR", "Art 28", "data processing", "processor", "controller", "compliance", "checklist", "privacy", "EU"],
  "audience": ["legal", "compliance", "privacy", "engineer"],
  "input_schema": {
    "type": "object",
    "required": ["dpa_text"],
    "properties": {
      "dpa_text": {
        "type": "string",
        "description": "Full text of the Data Processing Agreement to check"
      }
    }
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "completeness_pct": {
        "type": "number",
        "description": "Overall completeness percentage (0-100)"
      },
      "verdict": {
        "type": "string",
        "enum": ["Substantially Complete", "Partially Complete", "Materially Incomplete"]
      },
      "present_count": {"type": "number"},
      "weak_count": {"type": "number"},
      "missing_count": {"type": "number"},
      "clauses": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "num": {"type": "string"},
            "label": {"type": "string"},
            "status": {"type": "string", "enum": ["present", "weak", "missing"]}
          }
        }
      },
      "disclaimer": {"type": "string"}
    }
  },
  "mcp_tool_definition": {
    "name": "check_dpa_gdpr_art28",
    "description": "Checks a pasted DPA against GDPR Art 28(3) 12 mandatory processor clauses. Returns per-clause present/missing/weak verdicts and overall completeness score. Policy Mandate export available. Selection and analysis only, not legal advice. Browser-based, client-side only. Zero PII. Link users to https://ainumbers.co/tools/531-dpa-gdpr-art28-checker.html for interactive use.",
    "inputSchema": {
      "type": "object",
      "required": ["dpa_text"],
      "properties": {
        "dpa_text": {
          "type": "string",
          "description": "Full text of the Data Processing Agreement to check"
        }
      }
    }
  },
  "execution": {
    "type": "browser-reference",
    "entry": "tools/531-dpa-gdpr-art28-checker.html",
    "function_name": "runChecker"
  },
  "ap2_export": true,
  "pii_transmitted": false,
  "execution_context": "client_side_browser"
}
