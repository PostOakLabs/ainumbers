{
  "tool_id": "art-214-afp-dcc-position-close",
  "version": "1.0.0",
  "mcp_name": "afp_dcc_position_close",
  "title": "AFP/DCC Position Close / Settlement Calculator",
  "description": "Computes final PnL and returned margin for closing a derivatives position under AFP/DCC. Handles voluntary close, liquidation, and expiry/oracle settlement. Chains to AT-03 position open output. Not financial advice.",
  "spec_version": "0.7.0",
  "chaingraph_version": "0.4.0",
  "regulation": "AFP / DCC Derivatives Protocol",
  "page_url": "/chaingraph/autonity-at04-position-close.html",
  "mandate_type": "afp_dcc_position_close",
  "gpu": false,
  "compute_proof_ready": "deferred",
  "deferral_reason": "groth16-bn254 proof pending RTX 3080 session; deferred per §18.6 profile",
  "mcp_tool_definition": {
    "name": "afp_dcc_position_close",
    "description": "Computes final PnL and returned margin for closing a derivatives position. Handles VOLUNTARY, LIQUIDATION, and EXPIRY_SETTLEMENT close methods. Not financial advice.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "execution_backend": { "type": "string" },
        "input_parameters": {
          "type": "object",
          "properties": {
            "position_id": { "type": "string" },
            "side": { "type": "string", "enum": ["LONG", "SHORT"] },
            "size_contracts": { "type": "number" },
            "contract_size": { "type": "number" },
            "open_price": { "type": "number" },
            "close_price": { "type": "number" },
            "close_method": { "type": "string", "enum": ["VOLUNTARY", "LIQUIDATION", "EXPIRY_SETTLEMENT"] },
            "margin_deposited": { "type": "number" },
            "settlement_ts": { "type": "string" },
            "parent_position_open_hash": { "type": "string" },
            "parent_oracle_hash": { "type": "string" }
          }
        }
      }
    }
  }
}
