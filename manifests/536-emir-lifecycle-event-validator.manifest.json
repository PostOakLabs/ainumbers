{
  "tool_id": "art-157-emir-lifecycle-event-validator",
  "version": "1.0.0",
  "title": "EMIR Lifecycle Event Validator",
  "description": "Validate an EMIR Refit action type against the prior reported state of the UTI: New/Position are legal on a previously unreported trade; Modify/Correct/Valuation/Terminate/Error require a prior open trade; Revive/Correct/Error apply to terminated trades. Catches the most common Refit rejection cause — illegal action-type sequences — before Trade Repository submission. Returns validity verdict and allowed action types for the current state. Zero network, zero PII.",
  "category": "chaingraph",
  "tags": ["emir", "emir-refit", "lifecycle", "action-type", "trade-repository", "state-machine", "derivatives", "wave28"],
  "audience": ["compliance", "developer", "agent"],
  "input_schema": {
    "type": "object",
    "properties": {
      "action_type": { "type": "string", "description": "Proposed EMIR action type: New, Modify, Correct, Terminate, Valuation, Error, Position, Revive" },
      "prior_state": { "type": "string", "description": "Prior reported state of the UTI: none, open, terminated" }
    },
    "required": ["action_type", "prior_state"]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "valid": { "type": "boolean" },
      "action_type": { "type": "string" },
      "prior_state": { "type": "string" },
      "allowed_actions": { "type": "array", "items": { "type": "string" } },
      "rejection_reason": { "type": "string" }
    }
  },
  "mcp_tool_definition": {
    "name": "validate_emir_lifecycle_event",
    "description": "Validate an EMIR Refit action type against the prior UTI state: New/Position on unreported, Modify/Correct/Valuation/Terminate/Error on open, Revive/Correct/Error on terminated. Returns validity verdict, allowed actions for the state, and rejection reason if invalid. Zero network, zero PII.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "action_type": { "type": "string", "description": "Proposed EMIR action type: New, Modify, Correct, Terminate, Valuation, Error, Position, Revive" },
        "prior_state": { "type": "string", "description": "Prior reported state of the UTI: none, open, terminated" }
      },
      "required": ["action_type", "prior_state"]
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "chaingraph/art-157-emir-lifecycle-event-validator.html",
    "function_name": "run",
    "timeout_ms": 5000
  },
  "ap2_export": true,
  "chaingraph": true
}
