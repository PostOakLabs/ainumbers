{
  "tool_id": "cry-05-agent-action-audit-trail-aggregator",
  "version": "1.0.0",
  "title": "Agent-Action Audit-Trail Aggregator",
  "description": "The regulatory receipt. Aggregates N execution_hashes from an agent session into one SHA-256 Merkle-root session receipt with per-leaf inclusion proofs and an ordered chain-depth map. Sets session_receipt_root. The tamper-evident audit object for EU AI Act Art. 12 record-keeping + DORA. Consumes ANY ChainGraph artifact; feeds CRY-04, PTG-01.",
  "category": "chaingraph",
  "tags": [
    "Merkle",
    "SHA-256",
    "session receipt",
    "audit trail",
    "provenance",
    "EU AI Act",
    "Article 12",
    "DORA",
    "tamper-evident",
    "OpenChainGraph"
  ],
  "input_schema": {
    "type": "object",
    "required": [
      "artifacts"
    ],
    "properties": {
      "artifacts": {
        "type": "array"
      }
    },
    "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
  },
  "mcp_tool_definition": {
    "name": "aggregate_execution_receipts",
    "description": "Aggregates N OpenChainGraph artifacts (or bare execution_hash strings) collected across an agent session into a single tamper-evident session receipt: a SHA-256 Merkle root over the ordered leaf execution hashes, a per-leaf inclusion proof (sibling path), and an ordered chain-depth map. Sets session_receipt_root. Exports an AP2 cryptographic_mandate. EU AI Act Art.12 / DORA record-keeping motivation. chain.parent_hashes = input leaf hashes; chain_depth = max(parent depths)+1.",
    "inputSchema": {
      "type": "object",
      "required": [
        "artifacts"
      ],
      "properties": {
        "artifacts": {
          "type": "array"
        }
      },
      "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "chaingraph/cry-05-agent-action-audit-trail-aggregator.html",
    "function_name": "TODO_FUNCTION_NAME_REVIEW",
    "timeout_ms": 5000
  },
  "ap2_export": true,
  "output_schema": {
    "type": "object",
    "properties": {
      "aggregator_chain_depth": {
        "type": "integer"
      },
      "all_proofs_verified": {
        "type": "boolean"
      },
      "max_chain_depth": {
        "type": "integer"
      },
      "merkle_root": {
        "type": "string"
      },
      "n_receipts": {
        "type": "integer"
      },
      "receipts": {
        "type": "array"
      },
      "session_receipt_root": {
        "type": "string"
      },
      "tree_depth": {
        "type": "integer"
      }
    }
  },
  "source": "generated draft — scripts/generate-node-manifest.mjs (MFSTGEN-1/NODECAT-WIRE-1, derivation tier 2); execution.function_name are TODO markers, no source found; category sourced from inline MANIFEST; output_schema is best-effort single-sample typing — review before treating as authoritative"
}
