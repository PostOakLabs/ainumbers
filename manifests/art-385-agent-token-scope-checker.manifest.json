{
  "tool_id": "art-385-agent-token-scope-checker",
  "version": "1.0.0",
  "title": "Agent Token Scope Checker",
  "description": "Compares a requested agent action (amount, currency, merchant category, timestamp) against an agent token or mandate's declared scope: spend cap, currency, MCC allow-list, and expiry. If an attenuation chain of ancestor tokens is supplied, also checks that each delegation link narrows -- never widens -- the parent's bounds. Returns an in-scope or out-of-scope verdict and receipt. Pure evaluation only -- never authorizes, blocks, or executes a payment. Consumes the same mandate-chain vocabulary as art-01; distinct job -- one requested action against one token's bounds, not full mandate-chain structural validation.",
  "category": "mandate-loop-work-mandate-compiler",
  "tags": [
    "TODO_TAGS_REVIEW"
  ],
  "input_schema": {
    "type": "object",
    "required": [
      "attenuation_chain"
    ],
    "properties": {
      "attenuation_chain": {
        "type": "array"
      },
      "requested_action": {
        "description": "type not evidenced by kernel source"
      },
      "token": {
        "description": "type not evidenced by kernel source"
      }
    }
  },
  "mcp_tool_definition": {
    "name": "check_agent_token_scope",
    "description": "Compare a requested agent action (amount, currency, merchant category, timestamp) against a token or mandate's declared scope: spend cap, currency, MCC allow-list, expiry, and optional attenuation-chain narrowing. Returns an in-scope or out-of-scope verdict and receipt. Pure evaluation — never authorizes, blocks, or executes a payment. Zero network.",
    "inputSchema": {
      "type": "object",
      "required": [
        "attenuation_chain"
      ],
      "properties": {
        "attenuation_chain": {
          "type": "array"
        },
        "requested_action": {
          "description": "type not evidenced by kernel source"
        },
        "token": {
          "description": "type not evidenced by kernel source"
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "chaingraph/art-385-agent-token-scope-checker.html",
    "function_name": "TODO_FUNCTION_NAME_REVIEW",
    "timeout_ms": 5000
  },
  "ap2_export": true,
  "output_schema": {
    "type": "object",
    "properties": {
      "attenuation_depth": {
        "type": "integer"
      },
      "checks_run": {
        "type": "integer"
      },
      "enforcement": {
        "type": "string"
      },
      "failing_checks": {
        "type": "array"
      },
      "token_id": {
        "type": "string"
      },
      "verdict": {
        "type": "string"
      }
    }
  },
  "source": "generated draft — scripts/generate-node-manifest.mjs (MFSTGEN-1/NODECAT-WIRE-1, derivation tier 2); tags, execution.function_name are TODO markers, no source found; category sourced from hub-categories.json cluster; output_schema is best-effort single-sample typing — review before treating as authoritative",
  "input_schema_provenance": "derived-from-kernel-reads 2026-09-01",
  "author": "PostOakLabs",
  "license": "CC-BY-4.0",
  "input_example": {
    "requested_action": {
      "amount": 50,
      "currency": "USD",
      "mcc": "5411",
      "requested_at": "2026-07-18T12:00:00Z"
    },
    "token": {
      "token_id": "tok_leaf_1",
      "max_amount": 100,
      "currency": "USD",
      "allowed_mccs": [
        "5411",
        "5412"
      ],
      "expires_at": "2026-08-01T00:00:00Z"
    }
  },
  "output_example": {
    "verdict": "IN_SCOPE",
    "checks_run": 5,
    "failing_checks": [],
    "token_id": "tok_leaf_1",
    "attenuation_depth": 0,
    "enforcement": "none"
  },
  "example_execution_hash": "4ece36acbd4cd1b130dc8cddc293d5a2d5494fb2d022e84b2dfe06ee554be87c"
}
