{
  "tool_id": "art-194-digest-manifest-builder",
  "version": "1.0.0",
  "title": "Digest Manifest Builder",
  "description": "Binds N file digests into one canonical, hash-anchored manifest. manifest_sha256 is SHA-256 over the JCS-canonical sorted entries array. This builds flat manifests with no tree, which is a different job from verify_merkle_batch (cry-04, which verifies Merkle proofs); BrowserChain's log owns Merkle trees. Also serves the plain checksum-tool use case. Returns the manifest plus checks for duplicate names, duplicate digests, malformed hex, and path-like names that are flagged and basenamed. Feeds the conversion receipt builder. Zero network, zero PII.",
  "category": "client-side-conversions",
  "tags": [
    "TODO_TAGS_REVIEW",
    "TODO_AP2_EXPORT_REVIEW"
  ],
  "input_schema": {
    "type": "object",
    "required": [],
    "properties": {
      "entries": {
        "type": "array"
      },
      "purpose": {
        "type": "unknown"
      },
      "sort": {
        "type": "unknown"
      }
    },
    "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
  },
  "mcp_tool_definition": {
    "name": "build_digest_manifest",
    "description": "Bind N file digests into one canonical, hash-anchored manifest. manifest_sha256 = SHA-256 over the JCS-canonical, deterministically sorted entries array. Returns manifest, checks[], all_checks_pass. Flat manifests only, no Merkle tree.",
    "inputSchema": {
      "type": "object",
      "required": [],
      "properties": {
        "entries": {
          "type": "array"
        },
        "purpose": {
          "type": "unknown"
        },
        "sort": {
          "type": "unknown"
        }
      },
      "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "chaingraph/art-194-digest-manifest-builder.html",
    "function_name": "TODO_FUNCTION_NAME_REVIEW",
    "timeout_ms": 5000
  },
  "ap2_export": false,
  "output_schema": {
    "type": "object",
    "properties": {
      "all_checks_pass": {
        "type": "boolean"
      },
      "checks": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "check": {
              "type": "string"
            },
            "detail": {
              "type": "string"
            },
            "pass": {
              "type": "boolean"
            }
          }
        }
      },
      "manifest": {
        "type": "object",
        "properties": {
          "entries": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "bytes": {
                  "type": "integer"
                },
                "media_type": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                },
                "sha256": {
                  "type": "string"
                }
              }
            }
          },
          "entry_count": {
            "type": "integer"
          },
          "manifest_sha256": {
            "type": "string"
          },
          "manifest_version": {
            "type": "string"
          },
          "purpose": {
            "type": "string"
          },
          "sort": {
            "type": "string"
          },
          "total_bytes": {
            "type": "integer"
          }
        }
      }
    }
  },
  "source": "generated draft — scripts/generate-node-manifest.mjs (MFSTGEN-1/NODECAT-WIRE-1, derivation tier 2); tags, execution.function_name, ap2_export are TODO markers, no source found; category sourced from hub-categories.json cluster; output_schema is best-effort single-sample typing — review before treating as authoritative"
}
