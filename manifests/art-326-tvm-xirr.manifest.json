{
  "tool_id": "art-326-tvm-xirr",
  "version": "1.0.0",
  "title": "XIRR (Irregular Dated Cash Flows)",
  "description": "Annualized rate of return for irregular-interval dated cash flows, matching Excel XIRR semantics exactly: fixed actual/365 day count, anchored to the first cash flow date, solved by deterministic bisection over a declared rate bracket. Companion to compute_irr for cash flows that do not fall on equal periods.",
  "category": "time-value-of-money-primitives",
  "tags": [
    "TODO_TAGS_REVIEW"
  ],
  "input_schema": {
    "type": "object",
    "required": [
      "cash_flows",
      "tolerance"
    ],
    "properties": {
      "bracket_hi": {
        "type": "number",
        "default": 10
      },
      "bracket_lo": {
        "type": "number",
        "default": -0.9999
      },
      "cash_flows": {
        "type": "array"
      },
      "max_iterations": {
        "type": "number",
        "default": 200
      },
      "tolerance": {
        "type": "number"
      }
    },
    "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
  },
  "mcp_tool_definition": {
    "name": "compute_xirr",
    "description": "Annualized rate of return for irregular-interval dated cash flows, Excel XIRR semantics (fixed actual/365 day count, anchored to the first cash flow date), solved by deterministic bisection.",
    "inputSchema": {
      "type": "object",
      "required": [
        "cash_flows",
        "tolerance"
      ],
      "properties": {
        "bracket_hi": {
          "type": "number",
          "default": 10
        },
        "bracket_lo": {
          "type": "number",
          "default": -0.9999
        },
        "cash_flows": {
          "type": "array"
        },
        "max_iterations": {
          "type": "number",
          "default": 200
        },
        "tolerance": {
          "type": "number"
        }
      },
      "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "chaingraph/art-326-tvm-xirr.html",
    "function_name": "TODO_FUNCTION_NAME_REVIEW",
    "timeout_ms": 5000
  },
  "ap2_export": true,
  "output_schema": {
    "type": "object",
    "properties": {
      "anchor_date": {
        "type": "string"
      },
      "bracket_hi_pct": {
        "type": "integer"
      },
      "bracket_lo_pct": {
        "type": "number"
      },
      "converged": {
        "type": "boolean"
      },
      "day_count_convention": {
        "type": "string"
      },
      "iterations": {
        "type": "integer"
      },
      "method": {
        "type": "string"
      },
      "note": {
        "type": "string"
      },
      "num_cash_flows": {
        "type": "integer"
      },
      "regulatory_basis": {
        "type": "string"
      },
      "tolerance": {
        "type": "number"
      },
      "xirr_pct": {
        "type": "number"
      }
    }
  },
  "source": "generated draft — scripts/generate-node-manifest.mjs (MFSTGEN-1/NODECAT-WIRE-1, derivation tier 2); tags, execution.function_name are TODO markers, no source found; category sourced from hub-categories.json cluster; output_schema is best-effort single-sample typing — review before treating as authoritative"
}
