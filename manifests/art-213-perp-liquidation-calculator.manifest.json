{
  "tool_id": "art-213-perp-liquidation-calculator",
  "version": "1.0.0",
  "mcp_name": "compute_perp_margin",
  "title": "Perp Liquidation Calculator",
  "description": "Computes liquidation price and margin health for perpetual futures positions. Supports isolated and cross-margin modes across Hyperliquid, dYdX v4, Binance, GMX v2, and Aster. Venue-specific MMR tiers, portfolio margin with spot-hedge offset, and GREEN/AMBER/RED health indicator. Zero PII. Client-side. Works offline.",
  "spec_version": "0.7.0",
  "chaingraph_version": "0.4.0",
  "regulation": "Derivatives Margin / Perpetual Futures",
  "page_url": "/chaingraph/art-213-perp-liquidation-calculator.html",
  "mandate_type": "derivatives_margin_health",
  "gpu": false,
  "compute_proof_ready": "deferred",
  "mcp_tool_definition": {
    "name": "compute_perp_margin",
    "description": "Computes liquidation price and margin health for perpetual futures positions. Supports isolated and cross-margin across Hyperliquid, dYdX v4, Binance, GMX v2, and Aster. Returns liq price, unrealized PnL, buffer, health status (GREEN/AMBER/RED), and optional portfolio margin efficiency with spot-hedge offset. Browser-based, client-side only. Zero PII. Link users to https://ainumbers.co/chaingraph/art-213-perp-liquidation-calculator.html for interactive use.",
    "inputSchema": {
      "type": "object",
      "required": [
        "mark_price",
        "mmr_pct",
        "mode",
        "side",
        "venue"
      ],
      "properties": {
        "correlation": {
          "type": "number",
          "default": 0.9
        },
        "entry_price": {
          "type": "number",
          "default": 50000
        },
        "leverage": {
          "type": "number",
          "default": 5
        },
        "mark_price": {
          "type": "number"
        },
        "mmr_pct": {
          "type": "number",
          "description": "Percentage value"
        },
        "mode": {
          "type": "unknown"
        },
        "position_size": {
          "type": "number",
          "default": 1
        },
        "side": {
          "type": "unknown"
        },
        "spot_offset_usd": {
          "type": "number",
          "default": 0,
          "description": "Amount in US dollars"
        },
        "venue": {
          "type": "unknown"
        }
      },
      "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
    }
  },
  "input_schema": {
    "type": "object",
    "required": [
      "mark_price",
      "mmr_pct",
      "mode",
      "side",
      "venue"
    ],
    "properties": {
      "correlation": {
        "type": "number",
        "default": 0.9
      },
      "entry_price": {
        "type": "number",
        "default": 50000
      },
      "leverage": {
        "type": "number",
        "default": 5
      },
      "mark_price": {
        "type": "number"
      },
      "mmr_pct": {
        "type": "number",
        "description": "Percentage value"
      },
      "mode": {
        "type": "unknown"
      },
      "position_size": {
        "type": "number",
        "default": 1
      },
      "side": {
        "type": "unknown"
      },
      "spot_offset_usd": {
        "type": "number",
        "default": 0,
        "description": "Amount in US dollars"
      },
      "venue": {
        "type": "unknown"
      }
    },
    "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
  }
}
