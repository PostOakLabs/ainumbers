{
  "tool_id": "157-settlement-orchestration-simulator",
  "version": "1.0.0",
  "title": "Settlement Orchestration Simulator",
  "description": "Model multi-leg payment settlement flows — configure batch timing, netting ratios, cut-off windows, and fail/retry logic to quantify liquidity requirements and settlement efficiency. Client-side. Zero PII.",
  "category": "embedded-finance-baas",
  "tags": [],
  "audience": [
    "payments",
    "compliance",
    "finance",
    "engineer"
  ],
  "input_schema": {
    "type": "object",
    "properties": {
      "scenarioName": {
        "type": "string",
        "description": "Scenario label"
      },
      "currency": {
        "type": "string",
        "description": "Settlement currency (ISO 4217)"
      },
      "legs": {
        "type": "string",
        "description": "Number of settlement legs"
      },
      "avgVal": {
        "type": "string",
        "description": "Average transaction value"
      },
      "dailyVol": {
        "type": "string",
        "description": "Daily transaction volume"
      },
      "failRate": {
        "type": "string",
        "description": "Expected fail rate (%)"
      },
      "method": {
        "type": "string",
        "description": "Settlement method (gross/net/hybrid)"
      },
      "batchFreq": {
        "type": "string",
        "description": "Batch frequency (continuous/hourly/eod)"
      },
      "settlWindow": {
        "type": "string",
        "description": "Settlement window (hours)"
      },
      "netting": {
        "type": "string",
        "description": "Netting enabled (true/false)"
      },
      "netRatio": {
        "type": "string",
        "description": "Expected netting ratio (%)"
      },
      "liqBuf": {
        "type": "string",
        "description": "Liquidity buffer (%)"
      }
    },
    "required": [
      "currency",
      "legs",
      "avgVal",
      "method"
    ]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "settledCount": {
        "type": "number"
      },
      "failedCount": {
        "type": "number"
      },
      "liquidityRequired": {
        "type": "number"
      },
      "mandateJson": {
        "type": "object"
      }
    }
  },
  "mcp_tool_definition": {
    "name": "settlement_orchestration_simulator",
    "description": "Model multi-leg payment settlement flows — configure batch timing, netting ratios, cut-off windows, and fail/retry logic to quantify liquidity requirements and settlement efficiency. Client-side. Zero PII. Browser-based, client-side only. Zero PII. Link users to https://ainumbers.co/tools/157-settlement-orchestration-simulator.html for interactive use.",
    "inputSchema": {
      "type": "object",
      "properties": {}
    }
  },
  "execution": {
    "type": "browser-reference",
    "entry": "tools/157-settlement-orchestration-simulator.html"
  },
  "ap2_export": false,
  "pii_transmitted": false,
  "execution_context": "client_side_browser"
}
