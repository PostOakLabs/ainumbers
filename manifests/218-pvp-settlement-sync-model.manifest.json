{
  "tool_id": "218-pvp-settlement-sync-model",
  "version": "1.0.0",
  "title": "PvP Settlement Synchronisation Model",
  "description": "Compare CLS, bilateral PvP, and sequential FX settlement. Settlement risk exposure, timing mismatch, netting efficiency. PvP design memo. Based on BIS PvP guidance (Nov 2022). Client-side. Zero PII.",
  "category": "cross-border-fx",
  "tags": [],
  "audience": [
    "payments",
    "compliance",
    "finance",
    "engineer"
  ],
  "input_schema": {
    "type": "object",
    "properties": {
      "pairSel": {
        "type": "string",
        "description": "Currency pair (e.g. EUR/USD)"
      },
      "notionalInput": {
        "type": "string",
        "description": "Notional amount per leg"
      },
      "volumeInput": {
        "type": "string",
        "description": "Daily trade volume (number of trades)"
      },
      "nettingInput": {
        "type": "string",
        "description": "Bilateral netting ratio (%)"
      },
      "windowInput": {
        "type": "string",
        "description": "Settlement window overlap (hours)"
      },
      "clsEligSel": {
        "type": "string",
        "description": "CLS-eligible (true/false)"
      }
    },
    "required": [
      "pairSel",
      "notionalInput"
    ]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "principalRisk": {
        "type": "number"
      },
      "clsEligibleVolume": {
        "type": "number"
      },
      "nettedExposure": {
        "type": "number"
      },
      "mandateJson": {
        "type": "object"
      }
    }
  },
  "mcp_tool_definition": {
    "name": "pvp_settlement_sync_model",
    "description": "Compare CLS, bilateral PvP, and sequential FX settlement. Settlement risk exposure, timing mismatch, netting efficiency. PvP design memo. Based on BIS PvP guidance (Nov 2022). Client-side. Zero PII. Browser-based, client-side only. Zero PII. Link users to https://ainumbers.co/tools/218-pvp-settlement-sync-model.html for interactive use.",
    "inputSchema": {
      "type": "object",
      "properties": {}
    }
  },
  "execution": {
    "type": "browser-reference",
    "entry": "tools/218-pvp-settlement-sync-model.html"
  },
  "ap2_export": true,
  "pii_transmitted": false,
  "execution_context": "client_side_browser"
}
