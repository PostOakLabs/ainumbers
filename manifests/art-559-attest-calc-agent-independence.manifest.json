{
  "tool_id": "art-559-attest-calc-agent-independence",
  "version": "1.0.0",
  "title": "Calculation-Agent Independence Attestation",
  "description": "Receipts the organizational-independence claim a parametric trigger's neutrality depends on: that the entity whose kernel computed a specific art-251/art-252/art-309 execution_hash declares no controlling or compensating relationship with a named interested party (cedant, sponsor, reinsurer, or other) to the outcome that trigger determines. Self-declared attestation only -- attests that independence was DECLARED, never that it was verified against an external corporate registry. A counterparty party_id is plaintext by default; a caller may instead supply it as a sha256-salted@1 commitment (SPEC.md §25) to withhold the identifier while still binding the attestation to it. Not a determination of independence and not a legal or regulatory finding.",
  "category": "insurance-and-parametric-trigger-adjudication",
  "tags": [
    "insurance",
    "parametric-trigger",
    "calc-agent-independence",
    "attestation",
    "cat-bond"
  ],
  "input_schema": {
    "type": "object",
    "properties": {
      "calc_agent_id": { "type": "string" },
      "interested_parties": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "party_role": { "type": "string", "enum": ["cedant", "sponsor", "reinsurer", "other"] },
            "party_id": { "type": "string" },
            "party_id_commitment_scheme": { "type": "string", "enum": ["sha256-salted@1"] }
          }
        }
      },
      "relationship_declaration": { "type": "string", "enum": ["none", "disclosed"] },
      "disclosure_note": { "type": "string" },
      "trigger_ref": {
        "type": "object",
        "properties": {
          "execution_hash": { "type": "string" },
          "tool_id": { "type": "string", "enum": ["art-251-compute-parametric-trigger-payout", "art-252-validate-cat-bond-trigger-terms", "art-309-parametric-index-deriver"] }
        }
      }
    }
  },
  "mcp_tool_definition": {
    "name": "attest_calc_agent_independence",
    "description": "Receipts the organizational-independence claim a parametric trigger's neutrality depends on: that the entity whose kernel computed a specific art-251/art-252/art-309 execution_hash declares no controlling or compensating relationship with a named interested party to the outcome that trigger determines. Self-declared only -- not a determination of independence.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "calc_agent_id": { "type": "string" },
        "interested_parties": { "type": "array" },
        "relationship_declaration": { "type": "string" },
        "disclosure_note": { "type": "string" },
        "trigger_ref": { "type": "object" }
      }
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "chaingraph/art-559-attest-calc-agent-independence.html",
    "function_name": "compute",
    "timeout_ms": 5000
  },
  "ap2_export": false,
  "output_schema": {
    "type": "object",
    "properties": {
      "calc_agent_id": { "type": "string" },
      "interested_parties": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "party_role": { "type": "string" },
            "party_id": { "type": ["string", "null"] },
            "label": { "type": "string" },
            "party_id_commitment_scheme": { "type": "string" }
          }
        }
      },
      "relationship_declaration": { "type": ["string", "null"] },
      "disclosure_note": { "type": ["string", "null"] },
      "trigger_ref": {
        "type": "object",
        "properties": {
          "execution_hash": { "type": ["string", "null"] },
          "tool_id": { "type": ["string", "null"] }
        }
      },
      "independence_asserted": { "type": "boolean" },
      "rejected_inputs": { "type": "array" },
      "note": { "type": "string" }
    }
  },
  "source": "PARAM-LIN-INDEP-1, WU-authored (D-class new-node shard) -- output_schema derived by running the kernel's own compute() directly against the 4 vectors in chaingraph/kernels/fixtures/art-559-attest-calc-agent-independence.fixtures.json, mirroring the NODEMANIFEST-BACKFILL-1 shape-B convention (no compute_proof exists yet, proving deferred to the async GPU queue). Not run through scripts/generate-node-manifest.mjs -- that generator reads the assembled chaingraph.json and this is a shard-only D-class WU that never touches the monolith (board/RIDER-KERNEL.md)."
}
