{
  "tool_id": "212-swift-gpi-rail-comparator",
  "version": "1.1.0",
  "title": "SWIFT gpi / SEPA Instant / FedNow / PIX Rail Comparator",
  "description": "Compare international and domestic real-time payment rails: SWIFT gpi, SEPA Instant, FedNow, PIX, UPI, PayTo, Faster Payments. Cost-speed-finality matrix, recommended rail, Policy Mandate JSON routing policy. Client-side. Works offline. Zero PII.",
  "category": "cross-border-fx",
  "tags": [],
  "audience": [
    "payments",
    "compliance",
    "finance",
    "engineer"
  ],
  "input_schema": {
    "type": "object",
    "properties": {
      "origin_region": {
        "type": "string"
      },
      "destination_region": {
        "type": "string"
      },
      "payment_type": {
        "type": "string",
        "enum": [
          "retail",
          "b2b",
          "highvalue",
          "remittance"
        ]
      },
      "urgency": {
        "type": "string",
        "enum": [
          "instant",
          "same_day",
          "next_day",
          "standard"
        ]
      }
    },
    "required": [
      "origin_region",
      "destination_region",
      "payment_type"
    ]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "recommended_rail": {
        "type": "object"
      },
      "scored_rails": {
        "type": "array"
      },
      "ap2_mandate": {
        "type": "object"
      }
    }
  },
  "mcp_tool_definition": {
    "name": "swift_gpi_rail_comparator",
    "description": "Compare international and domestic real-time payment rails: SWIFT gpi, SEPA Instant, FedNow, PIX, UPI, PayTo, Faster Payments. Cost-speed-finality matrix, recommended rail, Policy Mandate JSON routing policy. Client-side. Zero PII. Browser-based, client-side only. Zero PII. Link users to https://ainumbers.co/tools/212-swift-gpi-rail-comparator.html for interactive use.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "origin_region": {
          "type": "string"
        },
        "destination_region": {
          "type": "string"
        },
        "payment_type": {
          "type": "string",
          "enum": [
            "retail",
            "b2b",
            "highvalue",
            "remittance"
          ]
        },
        "urgency": {
          "type": "string",
          "enum": [
            "instant",
            "same_day",
            "next_day",
            "standard"
          ]
        }
      },
      "required": [
        "origin_region",
        "destination_region",
        "payment_type"
      ]
    }
  },
  "execution": {
    "type": "browser-reference",
    "entry": "tools/212-swift-gpi-rail-comparator.html",
    "function_name": "runComparison"
  },
  "ap2_export": true,
  "pii_transmitted": false,
  "execution_context": "client_side_browser",
  "prefill": true,
  "bridge_version": "1.0"
}
