{
  "tool_id": "art-134-agent-directory-publish-readiness",
  "version": "1.0.0",
  "title": "Agent Directory Publish Readiness Diagnostic",
  "description": "Emit a go/no-go directory publish-readiness verdict for Web Bot Auth / Visa TAP / Mastercard Agent Pay. Five boolean checks: well_known_path_ok, jwks_reachable, card_complete, rotation_posture_ok, alg_ed25519. ready = all true; gaps = failing checks.",
  "category": "chaingraph",
  "tags": ["web-bot-auth", "agent-identity", "publish-readiness", "visa-tap", "mastercard-agent-pay", "jwks", "wave24"],
  "audience": ["compliance", "developer", "agent"],
  "input_schema": {
    "type": "object",
    "properties": {
      "well_known_path_ok": { "type": "boolean" },
      "jwks_reachable": { "type": "boolean" },
      "card_complete": { "type": "boolean" },
      "rotation_posture_ok": { "type": "boolean" },
      "alg_ed25519": { "type": "boolean" }
    },
    "required": ["well_known_path_ok", "jwks_reachable", "card_complete", "rotation_posture_ok", "alg_ed25519"]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "ready": { "type": "boolean" },
      "gaps": { "type": "array", "items": { "type": "string" } },
      "checks": { "type": "object" }
    }
  },
  "mcp_tool_definition": {
    "name": "assess_agent_directory_publish_readiness",
    "description": "Emit go/no-go directory publish-readiness for Web Bot Auth / Visa TAP / MC Agent Pay. Five boolean checks. ready = all true; gaps = failing keys list.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "well_known_path_ok": { "type": "boolean" },
        "jwks_reachable": { "type": "boolean" },
        "card_complete": { "type": "boolean" },
        "rotation_posture_ok": { "type": "boolean" },
        "alg_ed25519": { "type": "boolean" }
      },
      "required": ["well_known_path_ok", "jwks_reachable", "card_complete", "rotation_posture_ok", "alg_ed25519"]
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "chaingraph/art-134-agent-directory-publish-readiness.html",
    "function_name": "run",
    "timeout_ms": 5000
  },
  "ap2_export": true,
  "chaingraph": true
}
