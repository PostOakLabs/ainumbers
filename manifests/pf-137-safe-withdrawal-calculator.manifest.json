{
  "tool_id": "pf-137-safe-withdrawal-calculator",
  "version": "1.0.0",
  "title": "Safe Withdrawal Rate Calculator",
  "description": "Model portfolio longevity under the 4% rule, dynamic withdrawal strategies, and sequence-of-returns risk. Compare fixed, flexible, and guardrail withdrawal approaches. Client-side. Zero PII.",
  "category": "personal-finance",
  "tags": [],
  "audience": [
    "payments",
    "compliance",
    "finance",
    "engineer"
  ],
  "input_schema": {
    "type": "object",
    "properties": {
      "portInput": {
        "type": "string",
        "description": "Portfolio value at retirement (currency amount)"
      },
      "annualInput": {
        "type": "string",
        "description": "Target annual withdrawal (currency amount)"
      },
      "retYearsInput": {
        "type": "string",
        "description": "Retirement duration (years)"
      },
      "nomRetInput": {
        "type": "string",
        "description": "Nominal portfolio return (%)"
      },
      "inflInput": {
        "type": "string",
        "description": "Inflation rate (%)"
      },
      "stressInput": {
        "type": "string",
        "description": "Stress scenario return (%)"
      },
      "guardUp": {
        "type": "string",
        "description": "Guardrail increase trigger (%)"
      },
      "guardDn": {
        "type": "string",
        "description": "Guardrail decrease trigger (%)"
      }
    },
    "required": [
      "portInput",
      "annualInput",
      "retYearsInput"
    ]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "safeWithdrawalRate": {
        "type": "number"
      },
      "depletionYear": {
        "type": "number"
      },
      "successProbability": {
        "type": "number"
      },
      "mandateJson": {
        "type": "object"
      }
    }
  },
  "mcp_tool_definition": {
    "name": "safe_withdrawal_calculator",
    "description": "Model portfolio longevity under the 4% rule, dynamic withdrawal strategies, and sequence-of-returns risk. Compare fixed, flexible, and guardrail withdrawal approaches. Client-side. Zero PII. Browser-based, client-side only. Zero PII. Link users to https://ainumbers.co/tools/pf-137-safe-withdrawal-calculator.html for interactive use.",
    "inputSchema": {
      "type": "object",
      "properties": {}
    }
  },
  "execution": {
    "type": "browser-reference",
    "entry": "tools/pf-137-safe-withdrawal-calculator.html"
  },
  "ap2_export": false,
  "pii_transmitted": false,
  "execution_context": "client_side_browser"
}
