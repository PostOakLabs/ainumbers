{
  "tool_id": "art-348-score-credit-model-quantized",
  "version": "1.0.0",
  "title": "Quantized Credit Model Scorer",
  "description": "Runs a fixed, int8-quantized logistic-regression-class credit-decisioning model as a pure integer inference kernel and returns the score it produced from the supplied normalized inputs. Proves that THIS fixed quantized model produced THIS score from THESE inputs. It is not a fairness attestation and not a model-quality certification, and the underlying model is a synthetic offline demand-test artifact, not fit for real regulatory credit decisioning. The quantization_parity block on the artifact records the float-vs-quantized agreement rate measured over 1000 held-out vectors.",
  "category": "quantized-credit-model-scoring",
  "tags": [
    "TODO_TAGS_REVIEW"
  ],
  "input_schema": {
    "type": "object",
    "required": [
      "normalized_fixp16"
    ],
    "properties": {
      "normalized_fixp16": {
        "type": "array"
      }
    },
    "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
  },
  "mcp_tool_definition": {
    "name": "score_credit_model_quantized",
    "description": "Runs a fixed, int8-quantized credit-decisioning model as a pure integer inference kernel and returns the score it produced from the supplied normalized inputs. Proves that THIS fixed quantized model produced THIS score from THESE inputs; not a fairness or model-quality attestation.",
    "inputSchema": {
      "type": "object",
      "required": [
        "normalized_fixp16"
      ],
      "properties": {
        "normalized_fixp16": {
          "type": "array"
        }
      },
      "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "chaingraph/art-348-score-credit-model-quantized.html",
    "function_name": "TODO_FUNCTION_NAME_REVIEW",
    "timeout_ms": 5000
  },
  "ap2_export": true,
  "output_schema": {
    "type": "object",
    "properties": {
      "accumulator_fixp": {
        "type": "integer"
      },
      "bits": {
        "type": "integer"
      },
      "decision": {
        "type": "integer"
      },
      "n_features": {
        "type": "integer"
      },
      "quant_method": {
        "type": "string"
      },
      "threshold_fixp": {
        "type": "integer"
      }
    }
  },
  "source": "generated draft — scripts/generate-node-manifest.mjs (MFSTGEN-1/NODECAT-WIRE-1, derivation tier 2); tags, execution.function_name are TODO markers, no source found; category sourced from hub-categories.json cluster; output_schema is best-effort single-sample typing — review before treating as authoritative"
}
