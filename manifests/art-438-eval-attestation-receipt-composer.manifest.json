{
  "tool_id": "art-438-eval-attestation-receipt-composer",
  "version": "1.0.0",
  "title": "Eval Attestation Receipt Composer",
  "description": "Hashes a third-party eval log (e.g. an Inspect AI transcript) and binds it into a receipt that a compiled Work Mandate (art-274) can reference. VERIFY-ONLY: never executes, re-runs, or re-scores the eval -- composes shipped §4 hash, §16 signature, and §20 anchor carriers around a digest the caller already produced. claim_strength is the weakest-link status across the eval log hash and the mandate reference, never inflated by one strong leg covering a missing other.",
  "category": "mandate-loop-work-mandate-compiler",
  "tags": [
    "TODO_TAGS_REVIEW"
  ],
  "input_schema": {
    "type": "object",
    "required": [
      "eval_log",
      "mandate_reference"
    ],
    "properties": {
      "eval_log": {
        "type": "unknown"
      },
      "mandate_reference": {
        "type": "unknown"
      }
    },
    "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
  },
  "mcp_tool_definition": {
    "name": "compose_eval_attestation_receipt",
    "description": "Hashes a third-party eval log (e.g. an Inspect AI transcript) and binds it into a receipt that a compiled Work Mandate can reference. VERIFY-ONLY: never re-runs or re-scores the eval.",
    "inputSchema": {
      "type": "object",
      "required": [
        "eval_log",
        "mandate_reference"
      ],
      "properties": {
        "eval_log": {
          "type": "unknown"
        },
        "mandate_reference": {
          "type": "unknown"
        }
      },
      "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "chaingraph/art-438-eval-attestation-receipt-composer.html",
    "function_name": "TODO_FUNCTION_NAME_REVIEW",
    "timeout_ms": 5000
  },
  "ap2_export": true,
  "output_schema": {
    "type": "object",
    "properties": {
      "attestation_determination": {
        "type": "string"
      },
      "claim_strength": {
        "type": "string"
      },
      "eval_format": {
        "type": "string"
      },
      "eval_id": {
        "type": "string"
      },
      "eval_log_hash": {
        "type": "string"
      },
      "mandate_reference": {
        "type": "object",
        "properties": {
          "policy_id": {
            "type": "string"
          },
          "work_mandate_hash": {
            "type": "string"
          }
        }
      },
      "not_proven": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "detail": {
              "type": "string"
            },
            "item": {
              "type": "string"
            }
          }
        }
      },
      "verify_instructions": {
        "type": "string"
      }
    }
  },
  "source": "generated draft — scripts/generate-node-manifest.mjs (MFSTGEN-1/NODECAT-WIRE-1, derivation tier 2); tags, execution.function_name are TODO markers, no source found; category sourced from hub-categories.json cluster; output_schema is best-effort single-sample typing — review before treating as authoritative"
}
