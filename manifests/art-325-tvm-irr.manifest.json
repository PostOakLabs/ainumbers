{
  "tool_id": "art-325-tvm-irr",
  "version": "1.0.0",
  "title": "Internal Rate of Return (IRR)",
  "description": "Internal rate of return for an equal-period cash flow series, solved by deterministic bisection over a declared rate bracket with declared tolerance and iteration cap. Never Newton/derivative-based, so no float-drift nondeterminism. Reports whether the bracket contained a sign change and whether the search converged.",
  "category": "time-value-of-money-primitives",
  "tags": [
    "TODO_TAGS_REVIEW"
  ],
  "input_schema": {
    "type": "object",
    "required": [
      "cash_flows",
      "tolerance"
    ],
    "properties": {
      "bracket_hi": {
        "type": "number",
        "default": 10
      },
      "bracket_lo": {
        "type": "number",
        "default": -0.9999
      },
      "cash_flows": {
        "type": "array"
      },
      "max_iterations": {
        "type": "number",
        "default": 200
      },
      "tolerance": {
        "type": "number"
      }
    },
    "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
  },
  "mcp_tool_definition": {
    "name": "compute_irr",
    "description": "Internal rate of return for an equal-period cash flow series, solved by deterministic bisection over a declared rate bracket with declared tolerance and iteration cap.",
    "inputSchema": {
      "type": "object",
      "required": [
        "cash_flows",
        "tolerance"
      ],
      "properties": {
        "bracket_hi": {
          "type": "number",
          "default": 10
        },
        "bracket_lo": {
          "type": "number",
          "default": -0.9999
        },
        "cash_flows": {
          "type": "array"
        },
        "max_iterations": {
          "type": "number",
          "default": 200
        },
        "tolerance": {
          "type": "number"
        }
      },
      "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "chaingraph/art-325-tvm-irr.html",
    "function_name": "TODO_FUNCTION_NAME_REVIEW",
    "timeout_ms": 5000
  },
  "ap2_export": true,
  "output_schema": {
    "type": "object",
    "properties": {
      "bracket_hi_pct": {
        "type": "integer"
      },
      "bracket_lo_pct": {
        "type": "number"
      },
      "converged": {
        "type": "boolean"
      },
      "irr_pct": {
        "type": "number"
      },
      "iterations": {
        "type": "integer"
      },
      "method": {
        "type": "string"
      },
      "note": {
        "type": "string"
      },
      "num_cash_flows": {
        "type": "integer"
      },
      "regulatory_basis": {
        "type": "string"
      },
      "tolerance": {
        "type": "number"
      }
    }
  },
  "source": "generated draft — scripts/generate-node-manifest.mjs (MFSTGEN-1/NODECAT-WIRE-1, derivation tier 2); tags, execution.function_name are TODO markers, no source found; category sourced from hub-categories.json cluster; output_schema is best-effort single-sample typing — review before treating as authoritative"
}
