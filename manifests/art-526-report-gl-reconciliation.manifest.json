{
  "tool_id": "art-526-report-gl-reconciliation",
  "version": "1.0.0",
  "title": "Report-to-General-Ledger Reconciliation",
  "description": "Ties a caller-declared reported figure to a caller-declared general-ledger figure, by account -- the only node in this reconciliation programme checked against an independent book of record rather than a second declared value. A designed plug (for example FR 2052a field S.B.6 Carrying Value Adjustment) is a first-class declared input, netted before the residual and never counted as a break. Appendix and supplemental-schedule semantics are versioned policy inputs, never hardcoded. Refuses to run where the requested reporting cadence is finer than the underlying GL schedule's own cadence, and treats a general ledger not yet declared closed as a distinct did_not_run outcome from a genuine tie-out break -- the failure that otherwise surfaces only at quarter-end. Emits a §27.4 gate-policy value plus a sibling execution_state at a predictable output_payload pointer. Clause: BCBS 239 §36(c), ECB RDARR Guide §3.5(1)/(2). Not a transaction matcher and not a filing tool.",
  "category": "reporting-reconciliation-and-source-data-quality",
  "tags": [
    "TODO_TAGS_REVIEW",
    "TODO_AP2_EXPORT_REVIEW"
  ],
  "input_schema": {
    "type": "object"
  },
  "mcp_tool_definition": {
    "name": "reconcile_report_to_general_ledger",
    "description": "Ties a caller-declared reported figure to a caller-declared general-ledger figure, by account -- the only node in this reconciliation programme checked against an independent book of record rather than a second declared value. A designed plug (for example FR 2052a field S.B.6 Carrying Value Adjustment) is a first-class declared input, netted before the residual and never counted as a break. Appendix and supplemental-schedule semantics are versioned policy inputs, never hardcoded. Refuses to run where the requested reporting cadence is finer than the underlying GL schedule's own cadence, and treats a general ledger not yet declared closed as a distinct did_not_run outcome from a genuine tie-out break -- the failure that otherwise surfaces only at quarter-end. Emits a §27.4 gate-policy value plus a sibling execution_state at a predictable output_payload pointer. Clause: BCBS 239 §36(c), ECB RDARR Guide §3.5(1)/(2). Not a transaction matcher and not a filing tool.",
    "inputSchema": {
      "type": "object"
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "chaingraph/art-526-report-gl-reconciliation.html",
    "function_name": "TODO_FUNCTION_NAME_REVIEW",
    "timeout_ms": 5000
  },
  "ap2_export": false,
  "output_schema": {
    "type": "object",
    "properties": {
      "as_of": { "type": "string" },
      "currency": { "type": "string" },
      "appendix_schedule_version": { "type": "string" },
      "appendix_schedule_source": { "type": "string" },
      "reporting_cadence": { "type": "string" },
      "schedule_cadence": { "type": "string" },
      "cadence_refused": { "type": "boolean" },
      "gl_closed_declared": { "type": "boolean" },
      "gl_closed": { "type": "boolean" },
      "gl_as_of": { "type": "string" },
      "account_count": { "type": "integer" },
      "accounts": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "account_id": { "type": "string" },
            "reported_figure_minor_units": { "type": "integer" },
            "gl_figure_minor_units": { "type": "integer" },
            "designed_plug_minor_units": { "type": "integer" },
            "designed_plug_display": { "type": "string" },
            "designed_plug_reason_code": { "type": "string" },
            "tolerance_minor_units": { "type": "integer" },
            "residual_minor_units": { "type": "integer" },
            "residual_display": { "type": "string" },
            "within_tolerance": { "type": "boolean" }
          }
        }
      },
      "breaking_account_count": { "type": "integer" },
      "plugged_account_count": { "type": "integer" },
      "decision": {
        "type": "object",
        "properties": {
          "gate_policy": { "type": "string" },
          "execution_state": { "type": "string" },
          "reason": { "type": "string" }
        }
      },
      "rejected_inputs": { "type": "array" },
      "rationale": { "type": "array", "items": { "type": "string" } },
      "note": { "type": "string" }
    }
  },
  "source": "generated draft — scripts/generate-node-manifest.mjs (MFSTGEN-1/NODECAT-WIRE-1, derivation tier 3); output_schema ADDED by NODEMANIFEST-BACKFILL-1 (2026-08-02) as one of the 7 decision-pointer pilot nodes, shape B per OUTSCHEMA-DECISION-1 — no compute_proof exists yet (proving still pending), so the schema was derived by running the kernel's own compute() directly against all 4 vectors in chaingraph/kernels/fixtures/art-526-report-gl-reconciliation.fixtures.json, `gate_policy`/`execution_state` confirmed nested under `decision` as declared; tags, execution.function_name, ap2_export are TODO markers, no source found, left as-is per category/tags adjudication in flight; category sourced from hub-categories.json cluster"
}
