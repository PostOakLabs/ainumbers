{
  "tool_id": "art-213-afp-dcc-position-open",
  "version": "1.0.0",
  "mcp_name": "afp_dcc_position_open",
  "title": "AFP/DCC Position Open Simulator",
  "description": "Simulates opening a derivatives position under the AFP and DCC model. Computes margin requirements, validates deposit sufficiency, and generates a deterministic position identifier. Chains to AT-02 margin health output. Not financial advice.",
  "spec_version": "0.7.0",
  "chaingraph_version": "0.4.0",
  "regulation": "AFP / DCC Derivatives Protocol",
  "page_url": "/chaingraph/autonity-at03-position-open.html",
  "mandate_type": "afp_dcc_position_open",
  "gpu": false,
  "compute_proof_ready": "deferred",
  "deferral_reason": "groth16-bn254 proof pending RTX 3080 session; deferred per §18.6 profile",
  "mcp_tool_definition": {
    "name": "afp_dcc_position_open",
    "description": "Simulates opening a derivatives position under AFP/DCC. Computes notional, required margin, margin excess, and generates a deterministic position_id. Not financial advice.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "execution_backend": { "type": "string" },
        "input_parameters": {
          "type": "object",
          "properties": {
            "product_id": { "type": "string" },
            "account_id": { "type": "string" },
            "side": { "type": "string", "enum": ["LONG", "SHORT"] },
            "size_contracts": { "type": "number" },
            "margin_deposited": { "type": "number" },
            "venue_id": { "type": "string" },
            "intent_ts": { "type": "string" },
            "initial_margin_ratio": { "type": "number" },
            "contract_size": { "type": "number" },
            "parent_margin_health_hash": { "type": "string" }
          }
        }
      }
    }
  }
}
