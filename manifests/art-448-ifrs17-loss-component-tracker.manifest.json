{
  "tool_id": "art-448-ifrs17-loss-component-tracker",
  "version": "1.0.0",
  "title": "IFRS 17 Loss Component Roll-Forward Tracker",
  "description": "Tracks the IFRS 17 para 50 loss-component roll-forward across periods: opening balance, additional loss recognised on new onerous contracts, reversal from subsequent favourable experience (capped so it never reverses more than the available balance), other adjustments, and release to profit or loss (capped at the pre-release balance). Delta over validate_ifrs17_csm_rollforward (art-178), which resets the loss component to a single period's shortfall and does not track the component's own multi-period roll-forward or its release pattern. IFRS 17 para 50, BC323-BC326. NaN-safe numeric validation on all inputs. Zero network, zero PII.",
  "category": "insurance-ifrs-17-and-solvency-ii",
  "tags": [
    "TODO_TAGS_REVIEW",
    "TODO_AP2_EXPORT_REVIEW"
  ],
  "input_schema": {
    "type": "object",
    "required": [],
    "properties": {
      "loss_component": {
        "type": "unknown"
      }
    },
    "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
  },
  "mcp_tool_definition": {
    "name": "track_ifrs17_loss_component_rollforward",
    "description": "Track the IFRS 17 loss-component roll-forward across periods: opening + additional - capped reversal + other_adj = pre-release; release capped at pre-release balance; closing floored at zero. Flags reversal excess, release excess, and full reversal.",
    "inputSchema": {
      "type": "object",
      "required": [],
      "properties": {
        "loss_component": {
          "type": "unknown"
        }
      },
      "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "chaingraph/art-448-ifrs17-loss-component-tracker.html",
    "function_name": "TODO_FUNCTION_NAME_REVIEW",
    "timeout_ms": 5000
  },
  "ap2_export": false,
  "output_schema": {
    "type": "object",
    "properties": {
      "additional_lc": {
        "type": "integer"
      },
      "closing_lc": {
        "type": "integer"
      },
      "fully_reversed": {
        "type": "boolean"
      },
      "lc_valid": {
        "type": "boolean"
      },
      "opening_lc": {
        "type": "integer"
      },
      "other_adj": {
        "type": "integer"
      },
      "pre_release": {
        "type": "integer"
      },
      "release_to_pnl": {
        "type": "integer"
      },
      "reversal_lc": {
        "type": "integer"
      }
    }
  },
  "source": "generated draft — scripts/generate-node-manifest.mjs (MFSTGEN-1/NODECAT-WIRE-1, derivation tier 2); tags, execution.function_name, ap2_export are TODO markers, no source found; category sourced from hub-categories.json cluster; output_schema is best-effort single-sample typing — review before treating as authoritative"
}
