{
  "tool_id": "art-211-event-market-pnl",
  "version": "1.0.0",
  "mcp_name": "event_market_pnl",
  "title": "Event-Market PnL Calculator",
  "description": "Computes PnL for scalar/linear event contracts using strike, settlement value, unit value, and direction. Models AFP forecast futures, Kalshi scalar markets, and CME weather contracts. Not financial advice.",
  "spec_version": "0.7.0",
  "chaingraph_version": "0.4.0",
  "regulation": "AFP / Kalshi / CME Event Contracts",
  "page_url": "/chaingraph/autonity-at01-pnl.html",
  "mandate_type": "event_market_pnl",
  "gpu": false,
  "compute_proof_ready": "deferred",
  "deferral_reason": "groth16-bn254 proof pending RTX 3080 session; deferred per §18.6 profile",
  "mcp_tool_definition": {
    "name": "event_market_pnl",
    "description": "Computes PnL for scalar/linear event contracts using strike, settlement value, unit value, and side. Compatible with AFP forecast futures, Kalshi scalar markets, and CME weather contracts. Not financial advice.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "execution_backend": { "type": "string" },
        "input_parameters": {
          "type": "object",
          "properties": {
            "mode": { "type": "string", "enum": ["scalar"] },
            "preset": { "type": "string" },
            "underlying": { "type": "string" },
            "strike": { "type": "number" },
            "settlement_value": { "type": "number" },
            "unit_value": { "type": "number" },
            "n_contracts": { "type": "number" },
            "side": { "type": "string", "enum": ["LONG", "SHORT"] },
            "min_price": { "type": "number" },
            "max_price": { "type": "number" }
          }
        }
      }
    }
  }
}
