{
  "tool_id": "art-327-tvm-annuity",
  "version": "1.0.0",
  "title": "Annuity PV / FV / Payment Solver",
  "description": "Solves present value, future value, or payment for an ordinary annuity or annuity-due, given the other two plus rate and number of periods, using the standard closed-form annuity factor. Matches Excel PV/FV/PMT semantics including the due=true (beginning-of-period) adjustment.",
  "category": "time-value-of-money-primitives",
  "tags": [
    "TODO_TAGS_REVIEW"
  ],
  "input_schema": {
    "type": "object",
    "required": [
      "due",
      "solve_for"
    ],
    "properties": {
      "due": {
        "type": "boolean"
      },
      "fv": {
        "type": "number",
        "default": 0
      },
      "nper": {
        "type": "number",
        "default": 0
      },
      "pmt": {
        "type": "number",
        "default": 0
      },
      "pv": {
        "type": "number",
        "default": 0
      },
      "rate_pct": {
        "type": "number",
        "default": 0,
        "description": "Percentage value"
      },
      "solve_for": {
        "type": "string",
        "enum": [
          "fv",
          "pmt",
          "pv"
        ]
      }
    },
    "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
  },
  "mcp_tool_definition": {
    "name": "compute_annuity",
    "description": "Solves present value, future value, or payment for an ordinary annuity or annuity-due given the other two plus rate_pct and nper, matching Excel PV/FV/PMT semantics.",
    "inputSchema": {
      "type": "object",
      "required": [
        "due",
        "solve_for"
      ],
      "properties": {
        "due": {
          "type": "boolean"
        },
        "fv": {
          "type": "number",
          "default": 0
        },
        "nper": {
          "type": "number",
          "default": 0
        },
        "pmt": {
          "type": "number",
          "default": 0
        },
        "pv": {
          "type": "number",
          "default": 0
        },
        "rate_pct": {
          "type": "number",
          "default": 0,
          "description": "Percentage value"
        },
        "solve_for": {
          "type": "string",
          "enum": [
            "fv",
            "pmt",
            "pv"
          ]
        }
      },
      "x_schema_provenance": "derived-from-kernel-reads 2026-09-01"
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "chaingraph/art-327-tvm-annuity.html",
    "function_name": "TODO_FUNCTION_NAME_REVIEW",
    "timeout_ms": 5000
  },
  "ap2_export": true,
  "output_schema": {
    "type": "object",
    "properties": {
      "annuity_factor": {
        "type": "number"
      },
      "due": {
        "type": "boolean"
      },
      "fv": {
        "type": "integer"
      },
      "note": {
        "type": "string"
      },
      "nper": {
        "type": "integer"
      },
      "pmt": {
        "type": "integer"
      },
      "pv": {
        "type": "number"
      },
      "rate_pct": {
        "type": "number"
      },
      "regulatory_basis": {
        "type": "string"
      },
      "solved_for": {
        "type": "string"
      }
    }
  },
  "source": "generated draft — scripts/generate-node-manifest.mjs (MFSTGEN-1/NODECAT-WIRE-1, derivation tier 2); tags, execution.function_name are TODO markers, no source found; category sourced from hub-categories.json cluster; output_schema is best-effort single-sample typing — review before treating as authoritative"
}
