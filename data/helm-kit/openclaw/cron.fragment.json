{
  "_source": "HELM-MAINTENANCE-BUILD-SPEC §3.1 (pre-kit). TODO-KIT: replace byte-for-byte with helm/agent-kit/openclaw/cron.fragment.json when HELM-AGENT-KIT-1 lands.",
  "cron": [
    {
      "name": "helm-nightly-check",
      "schedule": "nightly",
      "command": "helmd check <pack> --glob \"extracts/*.json\" --json"
    }
  ],
  "heartbeat": {
    "command": "helmd status --json"
  }
}
