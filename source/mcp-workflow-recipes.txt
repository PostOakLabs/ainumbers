AINumbers claim-level evidence snapshot — mcp-workflow-recipes
claim: MCP page body prose: build_workflow_links covers '148 named multi-tool workflows' (sentinel workflows.recipes, body-prose occurrence).
count_key: workflows.recipes
source_path: mcp.html
anchor: COUNT:workflows.recipes
occurrence: 1
scope: Public MCP-page prose; first of two workflows.recipes occurrences (body prose + build_workflow_links table row). This entry binds the prose line only.
as_of: 2026-09-21
review_by: 2026-12-20
registered_by: EVREG-1 pilot
registered_at: 2026-09-21
---
The single line below is the exact source-surface line (trimmed) this claim
was registered against at as_of. If the live line drifts from it,
scripts/check-evidence-register.mjs goes red until the claim is re-reviewed
and re-validated with --revalidate=mcp-workflow-recipes.
---
<p>Every tool carries <code>readOnlyHint: true</code>: nothing mutates state, nothing writes anywhere. <!--COUNT:mcp.widgets-->16<!--/COUNT--> are flagship AINumbers tools served as interactive MCP Apps widgets; every live ChainGraph compute node is callable as well, alongside utility tools for catalog search, deep-links, validation, and linting. <code>build_workflow_links</code> returns ordered, prefill-ready deep-links for any of <!--COUNT:workflows.recipes-->148<!--/COUNT--> named multi-tool workflows. The server also exposes 29 guided workflow prompts (<code>prompts/list</code>) that walk an assistant through each composed chain end-to-end.</p>
