#!/bin/sh
# fv-gate pre-commit hook. Install: cp this file to .git/hooks/pre-commit (or point core.hooksPath here).
# Runs fv-gate against staged tree defaults; non-zero exit blocks the commit.
node "$(dirname "$0")/../bin/fv-gate.mjs" --summary --list-unfloored
