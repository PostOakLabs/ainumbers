// KERNELCI-1 throwaway test file: a brand-new, unregistered kernel file.
// Not referenced by any chaingraph.json node, so §17 coverage has nothing
// to compare it against and must stay green. Not merged.
export function compute() { return {}; }
