{
  "_comment": "SSOT for the always-report CI shape. Each key names a substantive gating workflow; its array is the SAME path-glob list used by that workflow's `push` trigger (paths:) and by scripts/check-ci-relevant.mjs to compute pull_request relevance. Keep this the ONLY place the list lives — the four workflow files and the relevance script both read from here, so the list can never silently diverge between push-trigger filtering and PR relevance computation. [rce-round3-proof-d]",
  "scripts-verify": [
    "scripts/**",
    "chaingraph/vm/scripts/**",
    "chaingraph/graph/chains/**",
    "chaingraph/graph/nodes/**",
    ".github/workflows/**"
  ],
  "html-verify": [
    "tools/**.html",
    "guides/**.html",
    "chaingraph/**.html",
    "*.html",
    ".github/workflows/html-verify.yml"
  ],
  "jsdoc-checkjs": [
    "chaingraph/kernels/**/*.mjs"
  ],
  "land-verify": [
    "chaingraph/chaingraph.json",
    "chaingraph/**.receipt.json",
    "scripts/compute-proof-baseline.json",
    "chaingraph/zkvm-compute-integrity.html",
    "chaingraph/chaingraph-hub.html",
    "chaingraph/kernels/**",
    "chaingraph/graph/nodes/**",
    "scripts/run-proptests.mjs",
    "scripts/check-fv-floor-coverage.mjs",
    "scripts/fv-floor-coverage-baseline.json",
    ".github/workflows/land-verify.yml"
  ]
}
