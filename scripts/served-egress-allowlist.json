[
  {
    "match": ".htaccess",
    "scope": "checksum",
    "reason": "Deployed-but-never-served Apache config class (manifest entries .htaccess, ledger/.htaccess, trust/.htaccess): the files must deploy (the deploy manifest verifies they landed) and the server must also REFUSE to serve them (standard .ht* 403 policy — serving them would leak directives). First sampled by the seeded byte-integrity leg 2026-09-14T04:53Z (run 34807685470, seed 1789361650, hit rate ~3/9681 per 25-sample run). Healed by MAIN-REGEN-SERVED-EGRESS-FIXPOINT-1, which also taught the not-served branch this allowlist scope."
  },
  {
    "match": ".gitkeep",
    "scope": "checksum",
    "reason": "Deployed-but-never-served git placeholder class (the origin refuses dotfiles with 403, same policy family as the .ht* class): deploy-checksums.txt lists every deployed file including placeholders, but the host can never serve one. First sampled by the seeded byte-integrity leg 2026-09-19T22:19Z on main 50e66d03 (run failed 24/25 byte-clean; the single red was chaingraph/kernels/__proptests__/.gitkeep HTTP 403, cf-cache-status none; curl confirms the 403 is deterministic while a missing normal file 404s). Healed by MAIN-SERVED-EGRESS-DOTFILE-FIXPOINT-1."
  }
]
