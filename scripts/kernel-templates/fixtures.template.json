{
  "tool_id": "{{TOOL_ID}}",
  "note": "SCAFFOLD placeholder (KERNEL-SCAFFOLD-1) -- compute() is not yet implemented, so this single vector exists only to give check-guest-builtin-safety.mjs / vm-parity-gate.mjs / the proptest floor's fixture oracle something to call. Replace with real golden vectors (policy_parameters + the output_payload compute() actually returns) once compute() is implemented. human_sign_off: PENDING.",
  "vectors": [
    {
      "name": "scaffold-placeholder",
      "policy_parameters": {},
      "output_payload": {}
    }
  ]
}
