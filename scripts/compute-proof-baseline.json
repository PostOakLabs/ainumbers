{
  "_comment": "Ratchet ceiling for §18 deferred gpu:false nodes (profile ocg-p18-deterministic). Counts only go DOWN. Regenerate with: node scripts/check-compute-proof-coverage.mjs --update-baseline. DEFERRAL: generate_iscc_code (art-201) is parked because its ISO 24138 pipeline (pure-JS BLAKE3 + minhash + simhash) interprets to ~1B+ zkVM guest cycles, exceeding the local proving budget; output correctness is guaranteed instead by the ISO 24138 conformance-vector gate, so the zkVM proof is low marginal value. Revisit if an off-box proving path (larger GPU or proving network) is provisioned. Wave 40 deferrals (art-236..240): proofs pending RTX session; real conformance fixtures with golden hashes in place.",
  "deferred": 6,
  "deferred_nodes": [
    "generate_iscc_code",
    "build_ai_decision_log_record",
    "validate_agent_audit_trail",
    "classify_annex3_decisioning_obligations",
    "test_bifsg_bias_thresholds",
    "assess_naic_ais_program_readiness"
  ]
}
