{
  "_comment": "Ratchet ceiling for §18 deferred gpu:false nodes (profile ocg-p18-deterministic). Counts only go DOWN within a cluster; new clusters may add deferred nodes. Regenerate with: node scripts/check-compute-proof-coverage.mjs --update-baseline. DEFERRAL: generate_iscc_code (art-201) is parked because its ISO 24138 pipeline (pure-JS BLAKE3 + minhash + simhash) interprets to ~1B+ zkVM guest cycles, exceeding the local proving budget; output correctness is guaranteed instead by the ISO 24138 conformance-vector gate, so the zkVM proof is low marginal value. Revisit if an off-box proving path (larger GPU or proving network) is provisioned. ART-215..220 (cluster 21, US Mortgage Compliance Core): Newton-Raphson APR solver + TRID + QM kernels deferred at baseline 1 (guest cycle budget TBD for finance solvers); correctness guaranteed by fixture gate vectors.",
  "deferred": 7,
  "deferred_nodes": [
    "generate_iscc_code",
    "compute_reg_z_appendix_j_apr",
    "compute_trid_tolerance_cure",
    "verify_trid_apr_accuracy",
    "check_qm_points_and_fees",
    "classify_qm_apr_apor_spread",
    "lookup_reg_z_thresholds"
  ]
}
