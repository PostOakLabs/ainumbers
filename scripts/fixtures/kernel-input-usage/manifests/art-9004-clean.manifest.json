{
  "tool_id": "art-9004-clean",
  "version": "1.0.0",
  "title": "fixture: clean (all four read shapes, every binding used)",
  "input_schema": {
    "type": "object",
    "properties": {
      "amount_cents": { "type": "number", "default": 0 },
      "risk_tier": { "type": "string", "default": "standard" },
      "region_code": { "type": "string", "default": "US" },
      "channel": { "type": "string" }
    }
  }
}
